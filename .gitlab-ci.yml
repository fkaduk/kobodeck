image: golang:latest

stages:
    - lint
    - build

format:
    stage: lint
    script:
        - go get github.com/golang/lint/golint
        - make lint

compile:
    stage: build
    script:
        - go get
        - make build
        - make tarball
    artifacts:
        paths:
        - build/
        when: always
